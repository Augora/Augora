{
  "name": "la-bete-politique-front",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/react": "^16.8.22",
    "@types/react-helmet": "^5.0.8",
    "@types/styled-components": "^4.1.16",
    "lbp-wrapper": "^0.7.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-helmet": "^5.2.1",
    "react-router-dom": "^5.0.1",
    "typescript": "^3.5.2",
    "styled-components": "^4.3.2"
  },
  "devDependencies": {
    "now": "^15.6.0",
    "react-scripts": "3.0.1",
    "source-map-explorer": "^2.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "now-build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "analyze": "source-map-explorer 'build/static/js/*.js'",
    "deploy-prod": "now alias --token=$NOW_TOKEN $(now --no-clipboard --token=$NOW_TOKEN --public) augora.fr",
    "deploy-qua": "now alias --token=$NOW_TOKEN $(now --no-clipboard --token=$NOW_TOKEN --public) preprod.augora.fr"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
